# 2026-09-21

## JM Reader 项目进度盘点（只读审查 + 一处编译修复）

**背景**：用户询问项目开发进度，做了一次全量代码/文档盘点。

**项目现状**：JM Reader（独立禁漫阅读器，fork PiKA 单源化），包名 com.jmread，versionName 0.1.0 / versionCode 1。
main 代码 67 个 Kotlin 文件 / 12,956 行；测试 4 个文件 / 321 行（23 个用例全绿）；docs 3 份共 712 行。

**修复**：`app/src/main/java/com/jmread/ui/MainScreen.kt` 缺 `import androidx.compose.foundation.layout.fillMaxWidth`，
导致 `compileDebugKotlin` 失败（AgeGate 第 330 行 `Modifier.fillMaxWidth()` Unresolved reference）。
补上 import 后 `:app:testDebugUnitTest` + `:app:assembleDebug` 均 BUILD SUCCESSFUL。

**盘点结论（缺口清单）**：
1. 项目**不是 git 仓库**（无 .git）——无版本控制，高风险，建议立即 `git init`。
2. `update.json` 缺失（UpdateManager 已实现，独立更新通道文件未落地）。
3. `tools/` 下的金丝雀脚本（docs 计划的 jm-api-check.mjs / jm-capability-probe.mjs / jm-shape-probe.mjs /
   jm-scramble-rule.py / dump-fixtures.mjs）未落地，现有 8 个 `_fix*.py` / `_edit_*.py` / `_flip.py` / `_age.py`
   均为一次性改文件补丁脚本。
4. 写侧能力位全 false（hasCloudFavouriteRead/Write、hasDailyCheckIn、hasCloudHistory、hasCommentWrite）——
   按设计卡测试账号，代码在但不渲染入口。
5. 与设计文档偏差两处：
   - 首页实际是「关注 / 排行榜 / 每周必看」三 Tab（默认排行榜），设计文档 §5.1 原定首页为浏览流 + 排行/周必看入口。
   - 官方标签墙实际入口在搜索页，设计文档原定在分类页给一级入口（CategoryScreen 内无 tag/block 渲染）。
6. 超出设计文档的额外功能：关键词关注流（FollowSettings + FollowManageScreen + 首页关注 Tab）、
   收藏的作者（AuthorFavourites）、已读筛选（ReadFilter）、图片乱序还原统计（设置页）。
7. 真机验收（产品设计 §8 的 12 项 checklist）无任何执行记录，docs 中无验收报告。
8. 无 release 构建 / 无 CI；`gbuild.sh` 未落地。

**下次继续的建议顺序**：git init → 补 update.json + 发布脚本 → 真机 checklist → 补金丝雀脚本。

---

## 续：git 初始化 + 金丝雀脚本落地 + 两个线上故障

**git**：JM 原本不是 git 仓库，已 `git init`，本地身份对齐 pika（wacilimonster /
wacilimonster@users.noreply.github.com）。两个提交：
- `419910b` 首次提交（113 文件 / 15952 行）
- `3002810` 修 /week/filter + 金丝雀 + 发布链路
远端尚未配置——pika 用 `git@github.com:wacilimonster-source/pika.git`，JM 的仓库名待用户确认。

**新建 tools/（此前只有 8 个一次性补丁脚本，已归档到 tools/oneoff/）**：
- `jm-lib.mjs` 协议公共库，与 JmCrypto/JmClient 逐行对应，带 8 项离线自检
- `jm-api-check.mjs` 端点存活 + 不变量 + 能力位复核，退出码即结论
- `jm-shape-probe.mjs` 字段形态漂移比对，基线 `tools/.jm-shape-baseline.json`
- `dump-fixtures.mjs` 拉真实响应覆盖 fixture（**未执行**，真实数据含成人向标题，是否入库交用户决定）

**线上实测（2026-09-21）关键结论**：
1. **P0 未修**：排行榜日榜 `o=mv_t`、周榜 `o=mv_w` 稳定返回 total=0 空列表；月榜 `o=mv_m`
   正常 total=3498。而首页默认 Tab 是排行榜、默认档位 H24→mv_t，**用户首屏空白**。
   未知 o 值不报错而是静默回落浏览流（total=10000），所以必须靠 total 值识别。待产品决策。
2. **P1 已修**：`/week/filter` 线上返回 `data.list`，客户端读 `data.content`；因
   `JmListData.content` 有 `= emptyList()` 默认值 → 不抛异常只静默空列表。
   修法：JmListData 加 `list` 字段 + 统一取值口 `val items get() = content.ifEmpty { list }`，
   JmRepository 6 处 + JmClient DA 倒序兜底全部改走 items。
3. **P1 待办**：8 份 DTO fixture 是手写样本（含「测试本子A」「评论者甲」等痕迹，共 5.6KB），
   不具备防漂移能力，与它声称要防的 D1 教训相悖。
4. **P2**：服务端对不存在端点返回 HTTP 200 + code 200 + `errorMsg="Not legal.xxx"`；
   JmClient 只判断 HTTP 码和首字符 `{`，不检查 code/errorMsg。
5. **P2**：列表项 `category_sub` 线上是 `{id:null,title:null}`，靠 `coerceInputValues` 兜住——
   该选项在此是承重的，与 ADR-6「移除宽容解析依赖」的意图冲突。

**已确认无误**：DTO 声明类型与线上真实类型逐字段吻合（列表项 + 详情全查过），
PiKA D1 的两类根因（String→Object、String→Array）在本项目不存在。
浏览流 80/页、total 恒 10000、125 页上限、四维搜索、CID/expinfo、负向端点全部符合基线。

**数据漂移**：标签墙 47 个（文档记 43）、月榜 total 3498（文档记 3328）。

**验证命令**：`node tools/jm-api-check.mjs`（17 请求约 6s，当前 30/32 通过，2 项失败即 P0）。
金丝雀脚本依赖 Node，本机用 `C:\Users\wacil\.workbuddy-ai\binaries\node\versions\22.22.2-2\node.exe`。

**发布链路**：release 构建配置早已存在；本次补了 `gbuild.sh` 与 `docs/release.md`。

---

## 续二：仓库确定 + 更新通道串台修复

**仓库已确定**（用户 2026-09-21 提供）：`git@github.com:wacilimonster-source/jmc.git`
- remote `origin` 已配置，SSH 可用，仓库存在且为空（`ls-remote --heads` 无分支）
- update.json 地址：`https://raw.githubusercontent.com/wacilimonster-source/jmc/main/update.json`
- APK 直链：`https://raw.githubusercontent.com/wacilimonster-source/jmc/main/jm-reader-v{版本}.apk`
- 约定：APK 提交到仓库根目录才能被 raw 直链访问（沿用 PiKA 做法）

**修掉一个跨项目串台 bug（P0）**：`UpdateManager.UPDATE_URL` 原本指向
`wacilimonster-source/pika`。后果：JM 会读到 PiKA 的 update.json（version 1.6.0 > JM 0.1.0），
提示用户「发现新版本」并下载安装 **PiKA 的 APK**，且 sha256 校验能通过（校验的就是 PiKA 那个包）。
已改指向 jmc。产品设计 §2 的「独立仓库、独立 update.json」当时漏了实现。

**本次一并修掉的其它项**（详见 `docs/verification-2026-09-21.md`）：
- F1 排行榜日/周榜空：参数名经两个参考库确认无误（`JMComic-Crawler-Python/jm_config.py`、
  `JMComic-qt/category_view.py`），按参考库的确切参数形态（`order=` 空 + `c=` 空、不发 `t`）复测仍空
  → 判定服务端数据缺失。处置：默认档位 H24→D30（月榜）、空态文案、`RankScreen` 补空态分支
  （`ComicGridView` 自身没有空态处理，原会渲染纯白区域）。
- F4 `decryptEnvelope` 增加 `errorMsg` 以 `Not legal` 开头则抛 `JmException`
- F5 `JmNamedItem.id/title` 改 `String? = null` + `titleText/idText` 取值口，
  新增内联 JSON 回归测试（手写 fixture 里没有 null，盖不住）

**参考项目位置**（排查协议问题时的金矿，都在 `G:/game/nw/pka/`）：
`JMComic-Crawler-Python`（协议常量最全）、`JMComic-qt`、`haka_comic`、`picacg-qt`、`comic-app`、`pika`。
其中 `pika/tools/pica-api-check.mjs` 是金丝雀脚本的原始范本。

